info:
  name: login
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:5000/api/auth/login
  body:
    type: json
    data: '{ "rollNumber": "2025071129", "password": "test1234" }'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
