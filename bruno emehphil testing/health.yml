info:
  name: health
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:5000/health
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
