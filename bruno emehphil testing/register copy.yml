info:
  name: register copy
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:5000/api/auth/register
  body:
    type: json
    data: |
      {
        "rollNumber": "2025021248",
        "displayName": "RajS",
        "password": "test1234"
      } 
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
