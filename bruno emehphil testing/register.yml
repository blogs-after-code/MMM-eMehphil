info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:5000/api/auth/register
  body:
    type: json
    data: |-
      {
        "rollNumber": "2025071129",
        "displayName": "Divyansh",
        "password": "test1234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
